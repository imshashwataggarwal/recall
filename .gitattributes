# Normalize line endings on commit; check out native on each platform.
* text=auto

# Shell scripts and Python must stay LF so they run on macOS/Linux.
*.sh   text eol=lf
*.py   text eol=lf

# PowerShell is Windows-side; keep CRLF.
*.ps1  text eol=crlf
